<div  id="message-<%= message.id %>">
  <i class="author">
    <span><%= message.user.username %></span>
    <small><%= message.created_at.strftime("%a %b %e at %l:%M%p") %></small>
  </i>
  <p class="message-container2"><%= message.content %></p>
</div>
